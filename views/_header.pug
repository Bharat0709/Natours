header.header
  nav.nav.nav--tours
    img.logo-hero(src='/img/logo-green.png' alt='Natours logo')
  .header__logo
    a.nav__elall(href='/') ALL TOURS
  nav.nav.nav--user
    if user
      a.nav__el.nav__el--logout Log out
      a.nav__el(href='/me')
        img.nav__user-img(src=`/img/users/${user.photo}` alt=`Photo of ${user.name}`)
        span= user.name.split(' ')[0]
    else
      a.nav__el(href='/login') Log In
      a.nav__el.nav__el--cta(href='/signup') Sign up

    script.
      console.log(user)